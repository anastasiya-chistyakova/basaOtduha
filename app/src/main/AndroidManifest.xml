<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basaotduha">

    <!-- Разрешения (на будущее, можно оставить) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="АРМ База отдыха"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Главное меню -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Бронирования -->
        <activity
            android:name=".BookingListActivity"
            android:exported="false" />

        <activity
            android:name=".BookingEditActivity"
            android:exported="false" />

        <!-- Услуги -->
        <activity
            android:name=".ServiceListActivity"
            android:exported="false" />

        <activity
            android:name=".ServiceEditActivity"
            android:exported="false" />

        <!-- Помещения -->
        <activity
            android:name=".RoomStatusActivity"
            android:exported="false" />

    </application>

</manifest>